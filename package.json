{
  "name": "opencode-fleet-v1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "description": "Lightweight orchestrator → workers for OpenCode v1 (file spool + prompt_async, takeover-friendly).",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.18.32"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.7.4"
  },
  "engines": {
    "node": ">=20"
  }
}
